<h1>Create new rating</h1>

<%= form_for(@rating) do |f| %>
    beer: <%= f.select :beer_id, options_from_collection_for_select(@beers, :id, :to_s) %>
    score: <%= f.number_field :score %>
    <%= f.submit %>
    <% end %>